{
  "name": "otrie",
  "description": "Observable Path Trie Store",
  "version": "1.1.2",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "private": false,
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./dist",
    "lint": "eslint . --ext .ts",
    "test": "jest --runInBand",
    "release": "npm run clean && npm run build && npm run lint && npm run test && rimraf ./dist/tests && npm publish"
  },
  "dependencies": {
    "assertic": "^1.0.0",
    "ptrie": "^1.0.1",
    "rxjs": "^7.5.7"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.54.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=10"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": [
    "observable",
    "reactive",
    "rxjs",
    "trie",
    "tree",
    "store",
    "state",
    "observable tree",
    "observable state",
    "observable store",
    "reactive tree",
    "reactive state",
    "reactive store"
  ],
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git@github.com:mfursov/otrie.git"
  }
}
